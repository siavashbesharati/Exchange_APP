@model IEnumerable<ForexExchange.Models.Currency>
@{
    ViewData["Title"] = "مدیریت ارزها";
}

<div class="container-fluid">
    <div class="row mb-3">
        <div class="col-12 d-flex justify-content-between align-items-center flex-wrap gap-2">
            <h1 class="h3 mb-0">مدیریت ارزها</h1>
            <div class="d-flex flex-wrap gap-2">
                <a asp-action="ManageDisplayOrder" class="btn btn-outline-primary">
                    <i class="fas fa-sort-amount-down"></i> ترتیب نمایش داشبورد
                </a>
                <a asp-action="ManageRatePriority" class="btn btn-outline-warning">
                    <i class="fas fa-bolt"></i> قدرت ارز (نرخ)
                </a>
                <a asp-action="Create" class="btn btn-primary"><i class="fas fa-plus"></i> افزودن ارز</a>
            </div>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="بستن"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="بستن"></button>
        </div>
    }

    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped align-middle">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>کد</th>
                            <th>نام</th>
                            <th>نام فارسی</th>
                            <th>نماد</th>
                            <th>ترتیب نمایش</th>
                            <th>قدرت (نرخ)</th>
                            <th>وضعیت</th>
                            <th>عملیات</th>
                        </tr>
                    </thead>
                    <tbody>
                    @foreach (var c in Model)
                    {
                        <tr>
                            <td>@c.Id</td>
                            <td><span class="badge @(c.Code == "IRR" ? "bg-secondary" : "bg-primary")">@Html.CurrencyCodeWithSymbol(c.Code, c.Symbol, 16)</span></td>
                            <td>@c.Name</td>
                            <td>@c.PersianName</td>
                            <td>
                                @if (CurrencyHelper.IsImageSymbol(c.Symbol))
                                {
                                    <img src="@c.Symbol" alt="@c.Code" style="height:24px;width:auto;vertical-align:middle;" />
                                }
                                else
                                {
                                    @c.Symbol
                                }
                            </td>
                            <td>@c.DisplayOrder</td>
                            <td>@c.RatePriority</td>
                            <td>
                                @if (c.IsActive)
                                {
                                    <span class="badge bg-success">فعال</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">غیرفعال</span>
                                }
                            </td>
                            <td>
                                <div class="btn-group">
                                    <a asp-action="Edit" asp-route-id="@c.Id" class="btn btn-sm btn-outline-secondary">ویرایش</a>
                                    <form asp-action="ToggleActive" asp-route-id="@c.Id" method="post" class="d-inline">
                                        <button type="submit" class="btn btn-sm @(c.IsActive ? "btn-outline-danger" : "btn-outline-success")">
                                            @(c.IsActive ? "غیرفعال" : "فعال")
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
}
