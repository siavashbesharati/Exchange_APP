@inject ForexExchange.Services.ISettingsService SettingsService
@{
    // Get dynamic branding data directly from service
    var websiteName = await SettingsService.GetWebsiteNameAsync();
    var companyName = await SettingsService.GetCompanyNameAsync();
    var logoDataUrl = await SettingsService.GetLogoDataUrlAsync();
}

<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>

    <meta charset="utf-8" />
    <meta name="viewport" content="@(ViewData["Viewport"] ?? "width=device-width, initial-scale=1.0")" />
    <title>@ViewData["Title"] - @websiteName</title>

    <!-- Open Graph Tags for WhatsApp and Social Media -->
    <meta property="og:title" content="@(ViewData["OGTitle"] ?? ViewData["Title"] ?? websiteName)" />
    <meta property="og:description"
          content="@(ViewData["OGDescription"] ?? $"{websiteName} - بهترین نرخ‌های روز، معاملات امن و سریع")" />
    <meta property="og:image" content="@logoDataUrl" />
    <meta property="og:image:alt" content="@(ViewData["OGImageAlt"] ?? $"لوگو {websiteName}")" />
    <meta property="og:url"
          content="@(ViewData["OGUrl"] ?? (Context.Request.Scheme + "://" + Context.Request.Host + Context.Request.Path))" />
    <meta property="og:type" content="@(ViewData["OGType"] ?? "website")" />
    <meta property="og:site_name" content="@websiteName" />
    <meta property="og:locale" content="fa_IR" />

    <!-- Twitter Card Tags -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="@(ViewData["TwitterTitle"] ?? ViewData["Title"] ?? websiteName)" />
    <meta name="twitter:description"
          content="@(ViewData["TwitterDescription"] ?? ViewData["OGDescription"] ?? $"{websiteName} - بهترین نرخ‌های روز، معاملات امن و سریع")" />
    <meta name="twitter:image" content="@(ViewData["TwitterImage"] ?? ViewData["OGImage"] ?? logoDataUrl)" />

    <!-- WhatsApp Specific -->
    <meta property="whatsapp:title" content="@(ViewData["OGTitle"] ?? ViewData["Title"] ?? websiteName)" />
    <meta property="whatsapp:description"
          content="@(ViewData["OGDescription"] ?? $"{websiteName} - بهترین نرخ‌های روز، معاملات امن و سریع")" />
    <meta property="whatsapp:image" content="@(ViewData["OGImage"] ?? logoDataUrl)" />

    <!-- Bootstrap 5 RTL -->
    <link href="~/refrence/bootstrap.rtl.min.css" rel="stylesheet">
        <link href="~/lib/fontawesome/all.css" rel="stylesheet">

            <!-- Font Awesome -->

            <!-- Animate.css for modern animations -->
            <link rel="stylesheet" href="~/refrence/animate.min.css">

                <!-- Select2 CSS -->
                <link href="~/refrence/select2.min.css" rel="stylesheet" />

                <!-- Persian Fonts -->
                <link href="~/fonts/font.css" rel="stylesheet">

                    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
                    <link rel="stylesheet" href="~/css/currency-formatter.css" asp-append-version="true" />
                    <link rel="stylesheet" href="~/css/modern-notifications.css" asp-append-version="true" />
                    <link rel="stylesheet" href="~/ForexExchange.styles.css" asp-append-version="true" />

                    <!-- Favicon -->
                    <link rel="apple-touch-icon" sizes="180x180" href="@logoDataUrl">
                    <link rel="icon" type="image/png" sizes="32x32" href="@logoDataUrl">
                    <link rel="icon" type="image/png" sizes="16x16" href="@logoDataUrl">
                    <link rel="manifest" href="@Url.Action("WebManifest", "Home")">
                    <link rel="shortcut icon" href="@logoDataUrl">
                    <meta name="msapplication-TileColor" content="#da532c">
                    <meta name="theme-color" content="#ffffff">

                    <style>
                            body {
                                font-family: 'Vazirmatn', sans-serif;
                                direction: rtl;
                                padding-top: 20px;
                            }

                            .navbar-brand {
                                font-weight: 600;
                            }

                            /* RTL SweetAlert2 Support */
                            .rtl-swal-popup {
                                direction: rtl;
                                text-align: right;
                            }

                            .rtl-swal-title {
                                text-align: center;
                            }

                            .rtl-swal-content {
                                text-align: right;
                            }

                            /* Admin Notification Modal Styling */
                            .admin-notification-modal {
                                font-family: 'Vazirmatn', sans-serif;
                                direction: rtl;
                            }

                            .admin-notification-modal .swal2-html-container {
                                text-align: right;
                            }

                            .admin-notification-modal .swal2-actions {
                                flex-direction: row-reverse;
                            }

                            .admin-notification-modal .swal2-confirm {
                                margin-left: 10px !important;
                                margin-right: 0 !important;
                            }

                            .admin-notification-modal .swal2-cancel {
                                margin-right: 10px !important;
                                margin-left: 0 !important;
                            }

                            /* Admin Notification Toast Styling (fallback) */
                            .admin-notification-toast {
                                font-family: 'Vazirmatn', sans-serif;
                                direction: rtl;
                            }

                            .admin-notification-toast .swal2-html-container {
                                text-align: right;
                            }

                            /* Connection status indicator */
                            .connection-status {
                                position: fixed;
                                bottom: 20px;
                                left: 20px;
                                z-index: 1000;
                                padding: 8px 12px;
                                border-radius: 20px;
                                font-size: 12px;
                                font-weight: 500;
                                display: none;
                            }

                            .connection-status.connected {
                                background-color: #d4edda;
                                color: #155724;
                                border: 1px solid #c3e6cb;
                            }

                            .connection-status.disconnected {
                                background-color: #f8d7da;
                                color: #721c24;
                                border: 1px solid #f5c6cb;
                            }

                            .connection-status.connecting {
                                background-color: #fff3cd;
                                color: #856404;
                                border: 1px solid #ffeaa7;
                            }

                            /* Floating Gear Button Styles */
                            .floating-gear-btn {
                                transition: all 0.3s ease;
                            }

                            .floating-gear-btn:hover {
                                color: #495057 !important;
                                transform: scale(1.1);
                            }

                            .floating-gear-btn:hover i {
                                animation: fastSpin 0.3s linear;
                            }

                            .floating-gear-btn.active {
                                color: #007bff !important;
                            }

                            @@keyframes fastSpin {
                                from {
                                    transform: rotate(0deg);
                                }

                                to {
                                    transform: rotate(360deg);
                                }
                            }

                            /* Currency Pool Mobile Styles */
                            .currency-pool-mobile-scroll .d-flex {
                                overflow-x: auto;
                                padding-bottom: 5px;
                                /* Webkit scrollbar styling */
                                scrollbar-width: thin;
                                scrollbar-color: #888 #f1f1f1;
                            }

                            .currency-pool-mobile-scroll .d-flex::-webkit-scrollbar {
                                height: 4px;
                            }

                            .currency-pool-mobile-scroll .d-flex::-webkit-scrollbar-track {
                                background: #f1f1f1;
                                border-radius: 2px;
                            }

                            .currency-pool-mobile-scroll .d-flex::-webkit-scrollbar-thumb {
                                background: #888;
                                border-radius: 2px;
                            }

                            .currency-pool-mobile-scroll .d-flex::-webkit-scrollbar-thumb:hover {
                                background: #555;
                            }

                            .currency-pool-mobile-item {
                                min-width: 80px;
                                margin-right: 10px;
                                flex-shrink: 0;
                                padding: 8px;
                                border: 1px solid #dee2e6;
                                border-radius: 8px;
                                background-color: #f8f9fa;
                            }

                            .currency-pool-mobile-item:last-child {
                                margin-right: 0;
                            }

                            .currency-pool-mobile-item .currency-pool-flag {
                                font-size: 1.2rem !important;
                                margin-bottom: 2px;
                            }

                            .currency-pool-mobile-item .currency-pool-code {
                                font-size: 0.7rem !important;
                            }

                            .currency-pool-mobile-item .currency-pool-balance {
                                font-size: 0.75rem !important;
                                font-weight: 600;
                                direction: ltr !important;
                            }

                            /* Ensure all currency pool balance elements use LTR direction */
                            .currency-pool-balance {
                                direction: ltr !important;
                            }
                    </style>
                    @RenderSection("Styles", required: false)
                </head>

                <body>

                    <!-- Connection Status Indicator -->
                    <div id="connectionStatus" class="connection-status">
                        <i class="fas fa-circle"></i>
                        <span id="connectionText">در حال اتصال...</span>
                    </div>

                    <div class="container" style="padding-bottom: 120px;">
                        <main role="main" class="pb-3">
                            @RenderBody()
                        </main>
                    </div>

                    @if (User.Identity?.IsAuthenticated == true)
                    {
                        @if (!User.IsInRole("Customer"))
                        {
                            <!-- Admin Footer with Currency Pool Section -->
                            <div class="fixed-bottom bg-light border-top shadow-lg" style="border-top: 3px solid #00000014 !important;">
                                <div class="container-fluid">
                                    <!-- Currency Pool Section -->
                                    <div class="currency-pool-section p-2 position-relative" id="currency-pool-section-admin"
                                         style="display: none;">
                                        <!-- Toggle button positioned absolutely - OUTSIDE collapsible content -->
                                        <button class="btn btn-sm btn-outline-secondary currency-pool-toggle position-absolute"
                                                onclick="toggleCurrencyPool()" title="بستن/باز کردن استخر ارز"
                                                style="top: -8px; right: 8px; z-index: 10; padding: 4px 8px;">
                                            <i class="fas fa-chevron-down" id="currency-pool-toggle-icon"></i>
                                        </button>

                                        <!-- Currency Pool Content (collapsible) -->
                                        <div class="currency-pool-content" id="currency-pool-content">

                                            <!-- Desktop version: Original row layout -->
                                            <div class="d-none d-md-block">
                                                <div class="row" id="currency-pool-row-admin">
                                                    <!-- Currency pool data will be loaded here via AJAX -->
                                                </div>
                                            </div>

                                            <!-- Mobile version toggle button -->
                                            <div class="d-md-none text-center mb-2">
                                                <button class="btn btn-sm btn-outline-secondary" onclick="toggleMobilePoolDisplay()"
                                                        style="padding: 2px 8px; font-size: 0.75rem;">
                                                    <i class="fas fa-exchange-alt me-1"></i>تغییر نمایش
                                                </button>
                                            </div>

                                            <!-- Mobile version: Option 1 - 3 columns per row -->
                                            <div class="d-md-none d-none" id="currency-pool-mobile-rows">
                                                <!-- Mobile rows will be populated here -->
                                            </div>

                                            <!-- Mobile version: Option 2 - Horizontal scrollable (default) -->
                                            <div class="d-md-none currency-pool-mobile-scroll" id="currency-pool-mobile-scroll">
                                                <div class="d-flex">
                                                    <!-- Mobile scroll items will be populated here -->
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Elegant separator line -->
                                    <div class="footer-separator"></div>

                                    <!-- Action Links Section -->
                                    <div class="p-3">
                                        <div class="row">
                                            <div class="col-12">
                                                <div class="d-flex justify-content-center flex-wrap gap-2">
                                                    <!-- Back to Dashboard -->
                                                    <a href="@Url.Action("Dashboard", "Home")"
                                                           class="btn btn-secondary btn-sm fw-normal flex-fill flex-sm-grow-0 @(ViewContext.RouteData.Values["action"]?.ToString() == "Dashboard" && ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "active" : "")"
                                                           style="background-color: #6c757d; border-color: #6c757d; color: white; text-shadow: 1px 1px 2px rgba(0,0,0,0.3); @(ViewContext.RouteData.Values["action"]?.ToString() == "Dashboard" && ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "box-shadow: 0 0 10px rgba(108, 117, 125, 0.5); transform: scale(1.05);" : "")">
                                                        <i class="fas fa-tachometer-alt me-1"></i>
                                                        <span class="d-none d-sm-inline"> داشبورد</span>
                                                        <span class="d-sm-none">داشبورد</span>
                                                    </a>

                                                    <!-- Add AccountDocument -->
                                                    <a href="@Url.Action("Upload", "AccountingDocuments")"
                                                           class="btn btn-success btn-sm fw-normal flex-fill flex-sm-grow-0 @(ViewContext.RouteData.Values["action"]?.ToString() == "Upload" && ViewContext.RouteData.Values["controller"]?.ToString() == "AccountingDocuments" ? "active" : "")"
                                                           style="background-color: #28a745; border-color: #28a745; color: white; text-shadow: 1px 1px 2px rgba(0,0,0,0.3); @(ViewContext.RouteData.Values["action"]?.ToString() == "Upload" && ViewContext.RouteData.Values["controller"]?.ToString() == "AccountingDocuments" ? "box-shadow: 0 0 10px rgba(40, 167, 69, 0.5); transform: scale(1.05);" : "")">
                                                        <i class="fas fa-file-invoice me-1"></i>
                                                        <span class="d-none d-sm-inline">ثبت سند</span>
                                                        <span class="d-sm-none">ثبت</span>
                                                    </a>

                                                    <!-- Create Order -->
                                                    <a href="@Url.Action("Create", "Orders")"
                                                           class="btn btn-primary btn-sm fw-normal flex-fill flex-sm-grow-0 @(ViewContext.RouteData.Values["action"]?.ToString() == "Create" && ViewContext.RouteData.Values["controller"]?.ToString() == "Orders" ? "active" : "")"
                                                           style="background-color: #007bff; border-color: #007bff; color: white; text-shadow: 1px 1px 2px rgba(0,0,0,0.3); @(ViewContext.RouteData.Values["action"]?.ToString() == "Create" && ViewContext.RouteData.Values["controller"]?.ToString() == "Orders" ? "box-shadow: 0 0 10px rgba(0, 123, 255, 0.5); transform: scale(1.05);" : "")">
                                                        <i class="fas fa-plus-circle me-1"></i>
                                                        <span class="d-none d-sm-inline">ثبت معامله جدید</span>
                                                        <span class="d-sm-none">معامله</span>
                                                    </a>

                                                    <!-- Reports -->
                                                    <a href="@Url.Action("Index", "Reports")"
                                                           class="btn btn-info btn-sm fw-normal flex-fill flex-sm-grow-0 @(ViewContext.RouteData.Values["controller"]?.ToString() == "Reports" ? "active" : "")"
                                                           style="background-color: #17a2b8; border-color: #17a2b8; color: white; text-shadow: 1px 1px 2px rgba(0,0,0,0.3); @(ViewContext.RouteData.Values["controller"]?.ToString() == "Reports" ? "box-shadow: 0 0 10px rgba(23, 162, 184, 0.5); transform: scale(1.05);" : "")">
                                                        <i class="fas fa-chart-bar me-1"></i>
                                                        <span class="d-none d-sm-inline">گزارشات</span>
                                                        <span class="d-sm-none">گزارش</span>
                                                    </a>

                                                    <!-- Tasks -->
                                                    <a href="@Url.Action("Index", "Tasks")"
                                                           class="btn btn-success btn-sm fw-normal flex-fill flex-sm-grow-0 @(ViewContext.RouteData.Values["controller"]?.ToString() == "Tasks" ? "active" : "")"
                                                           style="background-color: #28a745; border-color: #28a745; color: white; text-shadow: 1px 1px 2px rgba(0,0,0,0.3); @(ViewContext.RouteData.Values["controller"]?.ToString() == "Tasks" ? "box-shadow: 0 0 10px rgba(40, 167, 69, 0.5); transform: scale(1.05);" : "")">
                                                        <i class="fas fa-tasks me-1"></i>
                                                        <span class="d-none d-sm-inline">مدیریت وظایف</span>
                                                        <span class="d-sm-none">وظایف</span>
                                                    </a>

                                                </div>
                                            </div>
                                        </div>

                                        <!-- Mobile Management Button (visible only on mobile) -->
                                        <div class="d-sm-none mt-2">
                                            <a href="@Url.Action("Management", "Home")"
                                                   class="btn btn-warning btn-sm fw-normal w-100 @(ViewContext.RouteData.Values["action"]?.ToString() == "Management" && ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "active" : "")"
                                                   style="background-color: #ffc107; border-color: #ffc107; color: #212529; text-shadow: 1px 1px 2px rgba(255,255,255,0.3); @(ViewContext.RouteData.Values["action"]?.ToString() == "Management" && ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "box-shadow: 0 0 10px rgba(255, 193, 7, 0.5); transform: scale(1.05);" : "")">
                                                <i class="fas fa-cogs me-1"></i>
                                                مدیریت پیشرفته
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }

                        <!-- Desktop Floating Management Button (visible only on desktop for Admin/Operator) -->
                        @if (!User.IsInRole("Customer"))
                        {
                            <div class="d-none d-sm-block position-fixed" style="bottom: 16px; right: 20px; z-index: 1050;">
                                <a href="@Url.Action("Management", "Home")"
                                       class="floating-gear-btn @(ViewContext.RouteData.Values["action"]?.ToString() == "Management" && ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "active" : "")"
                                       style="color: #6c757d; font-size: 24px; text-decoration: none; display: flex; align-items: center; justify-content: center; width: 40px; height: 40px; transition: all 0.3s ease;">
                                    <i class="fas fa-gear"></i>
                                </a>
                            </div>
                        }
                        else if (User.IsInRole("Customer"))
                        {
                            <!-- Customer Profile Footer -->
                            <div class="fixed-bottom bg-white border-top shadow-lg p-3">
                                <div class="container-fluid">
                                    <div class="row">
                                        <div class="col-12">
                                            <div class="d-flex justify-content-center">
                                                <!-- Profile Link -->
                                                <a href="@Url.Action("Profile", "Account")" class="btn btn-primary">
                                                    <i class="fas fa-user me-2"></i>
                                                    پروفایل من
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <!-- Login action in Footer -->
                        <div class="fixed-bottom bg-white border-top shadow-lg p-3">
                            <div class="container-fluid">
                                <div class="row">
                                    <div class="col-12">
                                        <div class="d-flex justify-content-center">
                                            <a class="btn btn-primary" asp-area="" asp-controller="Account" asp-action="Login">
                                                <i class="fas fa-user me-2"></i>
                                                ورود به سیستم
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }

                    <footer class="border-top footer text-muted">
                        <div class="container">
                            &copy; @DateTime.Now.Year - @companyName
                        </div>
                    </footer>

                    <!-- Bootstrap 5 RTL JS -->
                    <script src="~/refrence/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo="
                            crossorigin="anonymous"></script>
                    <script src="~/refrence/bootstrap.bundle.min.js"></script>

                    <!-- Select2 JS -->
                    <script src="~/refrence/select2.min.js"></script>

                    <!-- Select2 Persian Language -->
                    <script src="~/js/select2-fa.js" asp-append-version="true"></script>

                    <!-- SweetAlert2 -->
                    <script src="~/refrence/sweetalert2.all.min.js"></script>

                    <!-- Microsoft SignalR -->
                    <script src="~/refrence/signalr.min.js"></script>


                    <script src="~/js/site.js" asp-append-version="true"></script>
                    <script src="~/js/admin-notifications.js" asp-append-version="true"></script>
                    <script src="~/js/web-push-manager.js" asp-append-version="true"></script>

                    <!-- Select2 Global Initialization -->
                    <script src="~/js/select2-init.js" asp-append-version="true"></script>

                    <!-- Universal Currency Formatter -->
                    <script src="~/js/currency-formatter.js" asp-append-version="true"></script>

                    <!-- Currency Amount Formatter -->
                    <script src="~/js/persian-number-converter.js" asp-append-version="true"></script>
                    <script src="~/js/currency-amount-formatter.js" asp-append-version="true"></script>
                    <script src="~/js/currency-form-helper.js" asp-append-version="true"></script>
                    <script src="~/js/auto-currency-display-formatter.js" asp-append-version="true"></script>



                    <!-- Currency Pool Footer Script -->
                    <script>
                            $(document).ready(function () {
                                loadCurrencyPoolData();
                                // Initialize pool state after a short delay to ensure DOM is ready
                                setTimeout(initializeCurrencyPoolState, 100);
                            });

                            function loadCurrencyPoolData() {
                                $.ajax({
                                    url: '@Url.Action("GetAllPoolsForFooter", "PoolManagement")',
                                    type: 'GET',
                                    success: function (response) {
                                        if (response.success && response.pools && response.pools.length > 0) {
                                            displayCurrencyPools(response.pools);
                                        } else {
                                            // Show currency pool section even with no data
                                            displayEmptyCurrencyPools();
                                        }
                                    },
                                    error: function (xhr, status, error) {
                                        console.error('Error loading currency pool data:', error);
                                        // Show currency pool section even on error
                                        displayEmptyCurrencyPools();
                                    }
                                });
                            }

                            // Currency to flag emoji mapping
                            function getCurrencyFlag(currencyCode) {
                                const currencyFlags = {
                                    'USD': '🇺🇸', // United States Dollar
                                    'EUR': '🇪🇺', // European Union Euro
                                    'GBP': '🇬🇧', // British Pound Sterling
                                    'JPY': '🇯🇵', // Japanese Yen
                                    'CHF': '🇨🇭', // Swiss Franc
                                    'CAD': '🇨🇦', // Canadian Dollar
                                    'AUD': '🇦🇺', // Australian Dollar
                                    'NZD': '🇳🇿', // New Zealand Dollar
                                    'SEK': '🇸🇪', // Swedish Krona
                                    'NOK': '🇳🇴', // Norwegian Krone
                                    'DKK': '🇩🇰', // Danish Krone
                                    'CNY': '🇨🇳', // Chinese Yuan
                                    'HKD': '🇭🇰', // Hong Kong Dollar
                                    'SGD': '🇸🇬', // Singapore Dollar
                                    'KRW': '🇰🇷', // South Korean Won
                                    'INR': '🇮🇳', // Indian Rupee
                                    'THB': '🇹🇭', // Thai Baht
                                    'MYR': '🇲🇾', // Malaysian Ringgit
                                    'IDR': '🇮🇩', // Indonesian Rupiah
                                    'PHP': '🇵🇭', // Philippine Peso
                                    'VND': '🇻🇳', // Vietnamese Dong
                                    'BRL': '🇧🇷', // Brazilian Real
                                    'MXN': '🇲🇽', // Mexican Peso
                                    'RUB': '🇷🇺', // Russian Ruble
                                    'TRY': '🇹🇷', // Turkish Lira
                                    'SAR': '🇸🇦', // Saudi Riyal
                                    'AED': '🇦🇪', // UAE Dirham
                                    'KWD': '🇰🇼', // Kuwaiti Dinar
                                    'QAR': '🇶🇦', // Qatari Riyal
                                    'BHD': '🇧🇭', // Bahraini Dinar
                                    'OMR': '🇴🇲', // Omani Rial
                                    'JOD': '🇯🇴', // Jordanian Dinar
                                    'LBP': '🇱🇧', // Lebanese Pound
                                    'EGP': '🇪🇬', // Egyptian Pound
                                    'MAD': '🇲🇦', // Moroccan Dirham
                                    'DZD': '🇩🇿', // Algerian Dinar
                                    'TND': '🇹🇳', // Tunisian Dinar
                                    'IQD': '🇮🇶', // Iraqi Dinar
                                    'SYP': '🇸🇾', // Syrian Pound
                                    'AFN': '🇦🇫', // Afghan Afghani
                                    'PKR': '🇵🇰', // Pakistani Rupee
                                    'BDT': '🇧🇩', // Bangladeshi Taka
                                    'LKR': '🇱🇰', // Sri Lankan Rupee
                                    'NPR': '🇳🇵', // Nepalese Rupee
                                    'BTN': '🇧🇹', // Bhutanese Ngultrum
                                    'IRR': '🇮🇷', // Iranian Toman
                                    'IRT': '🇮🇷', // Iranian Toman (same flag as IRR)
                                    'AMD': '🇦🇲', // Armenian Dram
                                    'AZN': '🇦🇿', // Azerbaijani Manat
                                    'GEL': '🇬🇪', // Georgian Lari
                                    'KZT': '🇰🇿', // Kazakhstani Tenge
                                    'KGS': '🇰🇬', // Kyrgyzstani Som
                                    'TJS': '🇹🇯', // Tajikistani Somoni
                                    'TMT': '🇹🇲', // Turkmenistani Manat
                                    'UZS': '🇺🇿', // Uzbekistani Som
                                    'ZAR': '🇿🇦', // South African Rand
                                    'NGN': '🇳🇬', // Nigerian Naira
                                    'KES': '🇰🇪', // Kenyan Shilling
                                    'GHS': '🇬🇭', // Ghanaian Cedi
                                    'ETB': '🇪🇹', // Ethiopian Birr
                                    'UGX': '🇺🇬', // Ugandan Shilling
                                    'TZS': '🇹🇿', // Tanzanian Shilling
                                    'RWF': '🇷🇼', // Rwandan Franc
                                    'XOF': '🌍', // West African CFA Franc
                                    'XAF': '🌍', // Central African CFA Franc
                                    'COP': '🇨🇴', // Colombian Peso
                                    'PEN': '🇵🇪', // Peruvian Sol
                                    'CLP': '🇨🇱', // Chilean Peso
                                    'ARS': '🇦🇷', // Argentine Peso
                                    'UYU': '🇺🇾', // Uruguayan Peso
                                    'PYG': '🇵🇾', // Paraguayan Guarani
                                    'BOB': '🇧🇴', // Bolivian Boliviano
                                    'VES': '🇻🇪', // Venezuelan Bolívar
                                    'GYD': '🇬🇾', // Guyanese Dollar
                                    'SRD': '🇸🇷', // Surinamese Dollar
                                    'FKP': '🇫🇰', // Falkland Islands Pound
                                    'BTC': '₿', // Bitcoin (using Bitcoin symbol)
                                    'ETH': 'Ξ', // Ethereum (using Ethereum symbol)
                                    'USDT': '₮', // Tether (using Tether symbol)
                                    'USDC': 'Ⓒ', // USD Coin
                                    'BNB': 'Ⓑ', // Binance Coin
                                    'ADA': 'Ⓐ', // Cardano
                                    'SOL': 'Ⓢ', // Solana
                                    'DOT': 'Ⓓ', // Polkadot
                                    'MATIC': 'Ⓜ', // Polygon
                                    'LTC': 'Ł', // Litecoin
                                    'DOGE': 'Ð', // Dogecoin
                                    'SHIB': '🐕', // Shiba Inu
                                    'AVAX': '🔺', // Avalanche
                                    'TRX': 'Ⓣ', // Tron
                                    'UNI': '🦄', // Uniswap
                                    'LINK': '🔗', // Chainlink
                                    'XRP': '⚡', // Ripple
                                    'XLM': '⭐', // Stellar
                                    'VET': '✔️', // VeChain
                                    'FIL': '📁', // Filecoin
                                    'THETA': 'Θ', // Theta Network
                                    'ICP': '∞', // Internet Computer
                                    'ALGO': '△', // Algorand
                                    'XTZ': 'ꜩ', // Tezos
                                    'ATOM': '⚛️', // Cosmos
                                    'CRO': '♢', // Crypto.com Coin
                                    'NEAR': '◯', // NEAR Protocol
                                    'SAND': '🏖️', // The Sandbox
                                    'MANA': '🎮', // Decentraland
                                    'AXS': '🎯', // Axie Infinity
                                    'ENJ': '🎲', // Enjin Coin
                                    'CHZ': '🏟️', // Chiliz
                                    'BAT': '🦇', // Basic Attention Token
                                    'ZIL': '◊', // Zilliqa
                                    'ONE': '1️⃣', // Harmony
                                    'HOT': '🔥', // Holo
                                    'IOST': '🌐', // IOST
                                    'ONT': '⭕', // Ontology
                                    'ICX': '❄️', // ICON
                                    'ZRX': '0️⃣', // 0x Protocol
                                    'REP': '📊', // Augur
                                    'KNC': '🔵', // Kyber Network Crystal
                                    'LRC': '⭕', // Loopring
                                    'SNX': '📈', // Synthetix Network Token
                                    'COMP': '🏛️', // Compound
                                    'YFI': '🏦', // yearn.finance
                                    'SUSHI': '🍣', // SushiSwap
                                    'CRV': '📈', // Curve DAO Token
                                    'AAVE': '👻', // Aave
                                    'MKR': '🏭', // Maker
                                    'REN': '🔄', // Republic Protocol
                                    'KSM': '🐦', // Kusama
                                    'DASH': '💨', // Dash
                                    'ZEC': '🛡️', // Zcash
                                    'XMR': '🕵️', // Monero
                                    'DCR': '💎', // Decred
                                    'SC': '☁️', // Siacoin
                                    'DGB': '⚡', // DigiByte
                                    'RVN': '🐦‍⬛', // Ravencoin
                                    'BTG': '🟨', // Bitcoin Gold
                                    'BCH': '💚', // Bitcoin Cash
                                    'BSV': '🔶', // Bitcoin SV
                                    'ETC': '💚', // Ethereum Classic
                                    'XEM': '🔶', // NEM
                                    'WAVES': '🌊', // Waves
                                    'LSK': '🔗', // Lisk
                                    'STRAT': '⚡', // Stratis
                                    'ARK': '🚢', // Ark
                                    'PIVX': '🔒', // PIVX
                                    'NAV': '⚡', // NavCoin
                                    'PART': '🔐', // Particl
                                    'BLK': '⚫', // BlackCoin
                                    'BLOCK': '🧱', // Blocknet
                                    'SYS': '⚙️', // Syscoin
                                    'VIA': '🛣️', // Viacoin
                                    'VTC': '🟢', // Vertcoin
                                    'GRS': '💎', // Groestlcoin
                                    'MONA': '🐱', // MonaCoin
                                    'DGD': '🏆', // DigixDAO
                                    'GNT': '🤖', // Golem
                                    'ANT': '🐜', // Aragon
                                    'BAL': '⚖️', // Balancer
                                    'BAND': '📻', // Band Protocol
                                    'NMR': '🧮', // Numeraire
                                    'MLN': '🚀', // Enzyme
                                    'LPT': '📹', // Livepeer
                                    'STORJ': '☁️', // Storj
                                    'GRT': '📊', // The Graph
                                    'OCEAN': '🌊', // Ocean Protocol
                                    'FET': '🤖', // Fetch.ai
                                    'AGIX': '🧠', // SingularityNET
                                    'RLC': '☁️', // iExec RLC
                                    'NKN': '🌐', // NKN
                                    'API3': '🔌', // API3
                                    'CELR': '⚡', // Celer Network
                                    'CTK': '🔐', // CertiK
                                    'DENT': '📱', // Dent
                                    'WIN': '🎰' // WINkLink
                                };

                                return currencyFlags[currencyCode] || '💱'; // Default to generic currency exchange emoji
                            }

                            function displayCurrencyPools(pools) {
                                // Clear all containers
                                $('#currency-pool-row-admin').empty();
                                $('#currency-pool-mobile-rows').empty();
                                $('#currency-pool-mobile-scroll .d-flex').empty();

                                // Calculate column width and offset based on number of currencies
                                const poolCount = pools.length;
                                let colClass = 'col-12';
                                let offsetClass = '';

                                if (poolCount <= 2) {
                                    colClass = 'col-6';
                                } else if (poolCount <= 3) {
                                    colClass = 'col-4';
                                } else if (poolCount <= 4) {
                                    colClass = 'col-3';
                                } else if (poolCount <= 6) {
                                    colClass = 'col-2';
                                } else if (poolCount === 7) {
                                    colClass = 'col-1';
                                    offsetClass = 'offset-2'; // Center 7 columns: (12-7)/2 = 2.5, rounded down to 2
                                } else if (poolCount === 8) {
                                    colClass = 'col-1';
                                    offsetClass = 'offset-2'; // Center 8 columns: (12-8)/2 = 2
                                } else if (poolCount === 9) {
                                    colClass = 'col-1';
                                    offsetClass = 'offset-1'; // Center 9 columns: (12-9)/2 = 1.5, rounded down to 1
                                } else if (poolCount === 10) {
                                    colClass = 'col-1';
                                    offsetClass = 'offset-1'; // Center 10 columns: (12-10)/2 = 1
                                } else if (poolCount === 11) {
                                    colClass = 'col-1';
                                    offsetClass = ''; // 11 columns, only 1 empty space, no offset needed
                                } else {
                                    colClass = 'col-1';
                                }

                                // Generate Desktop HTML
                                const desktopPoolsHtml = pools.map(function (pool, index) {
                                    const firstItemClass = index === 0 && offsetClass ? offsetClass : '';
                                    const flagEmoji = getCurrencyFlag(pool.currencyCode);
                                    return `
                                                            <div class="${colClass} ${firstItemClass} text-center border-end currency-pool-item">
                                                                <div class="currency-pool-flag" style="font-size: 1.5rem; margin-bottom: 2px;" title="${pool.currencyCode}">${flagEmoji}</div>
                                                                <div class="currency-pool-code text-muted small">${pool.currencyCode}</div>
                                                                <div class="currency-pool-balance ${pool.balance >= 0 ? 'text-success' : 'text-danger'}" data-no-format="true" data-skip-format="true" class="skip-auto-format">
                                                                    ${formatCurrency(pool.balance, pool.currencyCode)}
                                                                </div>
                                                            </div>
                                                        `;
                                }).join('');

                                // Generate Mobile Horizontal Scroll HTML
                                const mobileScrollHtml = pools.map(function (pool) {
                                    const flagEmoji = getCurrencyFlag(pool.currencyCode);
                                    return `
                                                            <div class="currency-pool-mobile-item text-center">
                                                                <div class="currency-pool-flag" title="${pool.currencyCode}">${flagEmoji}</div>
                                                                <div class="currency-pool-code text-muted">${pool.currencyCode}</div>
                                                                <div class="currency-pool-balance ${pool.balance >= 0 ? 'text-success' : 'text-danger'}" data-no-format="true" data-skip-format="true" class="skip-auto-format">
                                                                    ${formatCurrency(pool.balance, pool.currencyCode)}
                                                                </div>
                                                            </div>
                                                        `;
                                }).join('');

                                // Generate Mobile Rows HTML (3 columns per row)
                                let mobileRowsHtml = '';
                                for (let i = 0; i < pools.length; i += 3) {
                                    const rowPools = pools.slice(i, i + 3);
                                    mobileRowsHtml += '<div class="row mb-2">';
                                    rowPools.forEach(function (pool) {
                                        const flagEmoji = getCurrencyFlag(pool.currencyCode);
                                        mobileRowsHtml += `
                                                                <div class="col-4 text-center border-end currency-pool-item">
                                                                    <div class="currency-pool-flag" style="font-size: 1.2rem; margin-bottom: 2px;" title="${pool.currencyCode}">${flagEmoji}</div>
                                                                    <div class="currency-pool-code text-muted small">${pool.currencyCode}</div>
                                                                    <div class="currency-pool-balance ${pool.balance >= 0 ? 'text-success' : 'text-danger'}" style="font-size: 0.8rem;" data-no-format="true" data-skip-format="true" class="skip-auto-format">
                                                                        ${formatCurrency(pool.balance, pool.currencyCode)}
                                                                    </div>
                                                                </div>
                                                            `;
                                    });
                                    mobileRowsHtml += '</div>';
                                }

                                // Update all containers
                                $('#currency-pool-row-admin').html(desktopPoolsHtml);
                                $('#currency-pool-mobile-scroll .d-flex').html(mobileScrollHtml);
                                $('#currency-pool-mobile-rows').html(mobileRowsHtml);
                                $('#currency-pool-section-admin').show();
                            }

                            function displayEmptyCurrencyPools() {
                                // Clear all containers
                                $('#currency-pool-row-admin').empty();
                                $('#currency-pool-mobile-rows').empty();
                                $('#currency-pool-mobile-scroll .d-flex').empty();

                                // Show a message for empty pools
                                const emptyMessage = `
                                                        <div class="col-12 text-center text-muted py-2">
                                                            <small>هیچ استخر ارزی تعریف نشده است</small>
                                                        </div>
                                                    `;

                                $('#currency-pool-row-admin').html(emptyMessage);
                                $('#currency-pool-mobile-rows').html('<div class="text-center text-muted py-2"><small>هیچ استخر ارزی تعریف نشده است</small></div>');
                                $('#currency-pool-mobile-scroll .d-flex').html('<div class="text-center text-muted py-2 w-100"><small>هیچ استخر ارزی تعریف نشده است</small></div>');

                                // Always show the currency pool section
                                $('#currency-pool-section-admin').show();
                            }

                            function formatNumber(num) {
                                if (num === 0) return '0';
                                return num.toLocaleString('en-US', {
                                    minimumFractionDigits: 0,
                                    maximumFractionDigits: 2
                                });
                            }

                            // Function to toggle between mobile display modes
                            function toggleMobilePoolDisplay() {
                                const scrollContainer = $('#currency-pool-mobile-scroll');
                                const rowsContainer = $('#currency-pool-mobile-rows');

                                if (scrollContainer.hasClass('d-none')) {
                                    // Switch to scroll mode
                                    scrollContainer.removeClass('d-none');
                                    rowsContainer.addClass('d-none');
                                } else {
                                    // Switch to rows mode
                                    scrollContainer.addClass('d-none');
                                    rowsContainer.removeClass('d-none');
                                }
                            }

                            // Currency Pool Toggle Functionality
                            let isPoolMinimized = false;

                            function toggleCurrencyPool() {
                                const poolSection = $('#currency-pool-section-admin');
                                const poolContent = $('#currency-pool-content');
                                const toggleBtn = $('.currency-pool-toggle');
                                const toggleIcon = $('#currency-pool-toggle-icon');

                                if (isPoolMinimized) {
                                    // Maximize (show) the pool
                                    poolSection.removeClass('minimizing').addClass('maximizing');
                                    poolContent.removeClass('collapsed');
                                    toggleBtn.removeClass('collapsed');
                                    toggleIcon.removeClass('fa-chevron-up').addClass('fa-chevron-down');

                                    // Update state
                                    isPoolMinimized = false;

                                    // Store preference
                                    localStorage.setItem('currencyPoolMinimized', 'false');

                                    // Remove animation class after animation completes
                                    setTimeout(() => {
                                        poolSection.removeClass('maximizing');
                                    }, 400);

                                } else {
                                    // Minimize (hide) the pool
                                    poolSection.removeClass('maximizing').addClass('minimizing');
                                    poolContent.addClass('collapsed');
                                    toggleBtn.addClass('collapsed');
                                    toggleIcon.removeClass('fa-chevron-down').addClass('fa-chevron-up');

                                    // Update state
                                    isPoolMinimized = true;

                                    // Store preference
                                    localStorage.setItem('currencyPoolMinimized', 'true');

                                    // Remove animation class after animation completes
                                    setTimeout(() => {
                                        poolSection.removeClass('minimizing');
                                    }, 400);
                                }
                            }

                            // Initialize pool state from localStorage
                            function initializeCurrencyPoolState() {
                                const isMinimized = localStorage.getItem('currencyPoolMinimized') === 'true';

                                if (isMinimized) {
                                    const poolContent = $('#currency-pool-content');
                                    const toggleBtn = $('.currency-pool-toggle');
                                    const toggleIcon = $('#currency-pool-toggle-icon');

                                    // Set initial minimized state without animation
                                    poolContent.addClass('collapsed');
                                    toggleBtn.addClass('collapsed');
                                    toggleIcon.removeClass('fa-chevron-down').addClass('fa-chevron-up');
                                    isPoolMinimized = true;
                                }
                            }
                    </script>

                    @await RenderSectionAsync("Scripts", required: false)

                    <script>
                            // Set current user ID for client-side notification filtering
                            window.currentUserId = '@(User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value ?? "")';
                    </script>
                </body>

            </html>